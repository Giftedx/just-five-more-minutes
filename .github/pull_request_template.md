## Summary

<!-- What changed and why? One or two sentences is fine. -->

## Test plan

- [ ] `npm test` passes locally
- [ ] `npm run build` succeeds
- [ ] Played through manually (Room Mode / PC Mode as relevant)

## Screenshots / clips

<!-- Optional — especially for UI or 3D changes. Drop files in docs/screenshots/ if worth keeping. -->
